{
  "name": "tvcountdown",
  "repository": "git@github.com:novascreen/tvcountdown.git",
  "author": "Thomas Hermann <novascreen@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "lerna run --parallel dev",
    "build": "lerna run --parallel build",
    "deploy:api": "git subtree push --prefix packages/api heroku-api master",
    "deploy:web": "git subtree push --prefix packages/web heroku-web master"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^3.4.3"
  },
  "resolutions": {
    "graphql": "^14.0.2"
  }
}
