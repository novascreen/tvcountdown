enum Role {
  ADMIN
  USER
}

type User {
  id: ID! @unique
  email: String! @unique
  role: Role! @default(value: USER)
  name: String
  avatar: String
  auth0id: String! @unique
  identity: String
}
