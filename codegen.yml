schema: http://localhost:4000/graphql
generates:
  packages/app/src/graphql/types.ts:
    documents:
      - packages/app/src/{components,graphql,lib}/**/*.{ts,tsx}
    plugins:
      - typescript
      - typescript-operations
  packages/graphql/src/types.ts:
    config:
      contextType: ./utils#Context
    plugins:
      - typescript
      - typescript-resolvers
